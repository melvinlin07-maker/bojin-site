# 臉部張力系統（Face Tension Profile）— 設計規格與建置指示

> 交接文件。給：正在開發「撥筋美國網站」的 Claude Code session。
> 這份設計是網站主藍玉婷在另一個對話裡討論定案的，請照此建置。她是非技術背景，請用白話跟她確認、並主動守合規。
> 原則：先做看不見的地基（標準模板＋內容庫），最後才做看得到的頁面。

---

## 0. 這是什麼

把網站現有的 `quiz.html` 升級成一個「臉部張力側寫」個人化系統：
使用者做一個小測驗 → 得到一張五部位的張力雷達圖 → 系統自動組出一份專屬 3 分鐘撥筋儀式＋對應文章 → 收 email／導向 $79 課。

它同時是「免費大門口」，之後可延伸出低價付費版（$9–19 完整 7 天計畫）。

**⚠️ 它不是醫療診斷、不是相機掃臉。** 只是「自我感受問卷」，只描述緊繃／放鬆（tension／rested），不涉任何療效。

---

## 1. 核心原則：機器與內容分離，用「標籤」連接（這就是 JIG 標準模板）

**測驗結果永遠指向「標籤」，不指向某一篇特定文章。**

- 每篇文章 / 每個手法身上都貼一組固定標籤。
- 機器（測驗、雷達、組裝）**只讀標籤，不認任何一篇文章**。
- 之後新增第 10、50、100 篇文章時，只要**貼上同一套標籤**就自動被系統認得、自動可被抓出來 —— 機器一行都不用改。

現有 9 篇只是起點，內容會持續長大，所以這個「標籤層」必須先做對，這是整個系統成敗的關鍵。

---

## 2. JIG 標準模板：每篇內容必填的固定欄位

建議實作方式：建一個 `content-library.json`（單一內容庫），每篇一筆；或用每篇 HTML 的 frontmatter / `data-*` 屬性。二選一，但要**單一真實來源**。

| 欄位 | 值（用固定字彙） | 作用 |
|---|---|---|
| `id` | 檔名或唯一代號 | 識別 |
| `zone` ⭐ | `eyes` / `jaw-neck` / `cheeks-nasolabial` / `brows-forehead` / `glow-circulation`（五選一或多選） | **最關鍵接頭**：必須與雷達五軸完全一致 |
| `symptoms` | 例：`puffy`,`tension`,`tired`,`fine-lines`,`dullness` | 抓「剛好在講他困擾」的內容 |
| `type` | `routine-step`（手法步驟）或 `concept`（觀念文） | 決定被拿去組 routine 還是當背景知識 |
| `steps` | 有序步驟陣列（type=routine-step 時） | routine 由此拼出 |
| `level` | `beginner` / `advanced` | 新手先給簡單的 |
| `lang` | `zh` / `en` 各一份，且已過合規 | 一份餵兩個市場 |
| `compliance_ok` | `true` / `false` | false 者禁止被系統輸出 |
| `media` | 對應教學照 / 影片路徑 | 結果卡配圖用 |

⭐ **唯一鐵則**：`zone` 這欄用的字，必須跟雷達的五個軸一字不差。接頭統一，內容與機器就永遠對得上。

---

## 3. 架構（資料流）

```
使用者作答（點臉上緊繃處 + 3 題習慣）
        │
        ▼
   計分規則  ──►  五區張力分數(0–100)  ──►  雷達圖（讓人看見自己）
        │
        ▼
   依「zone 標籤」到內容庫抓料
        │
        ├─► 抓 type=routine-step 且 zone=最緊那區 → 組出 3 分鐘 routine
        ├─► 抓 type=concept 且 zone=最緊那區     → 當 email 背景知識
        ▼
   結果卡：個人化 routine ＋ 對應文章 ＋ 收 email（分流：眼睛的人進眼睛序列…）
```

上半是「固定不變的機器」，下半是「會一直長大的內容庫」。兩者只透過 `zone`／`symptoms` 標籤相接。

---

## 4. 建置順序（請照這個順序，先地基後門面）

1. **定 JIG**：把上表落成 `content-library.json` 的 schema，並跟藍玉婷用白話確認欄位。
2. **回填現有 9 篇**：幫每篇標好 `zone`/`symptoms`/`type`/`level`/`lang`，補上缺的中文或英文版。做完就產出一張「內容庫盤點表」給藍玉婷看（哪些部位已有文章、哪些部位還缺）。
3. **計分規則**：每題對應哪個 zone、加幾分 → 五區分數。
4. **組裝規則**：分數最高的 zone → 去內容庫抓對應 routine-step 拼 routine、抓對應 concept 文寄 email。
5. **最後才做 UI**：quiz 頁面 + 雷達圖（純 SVG，五軸）+ 結果卡。沿用 `styles.css` 既有元件與茶館風視覺。

**先交付第 1、2 步的成果（JIG 規格 + 9 篇回填盤點表），讓藍玉婷親眼看到「雷達→標籤→真實文章」接得起來，再往下做。**

---

## 5. 現有 9 篇的初步 zone 對應（給你當回填起點，請逐篇核對內文再定案）

| 檔案 | 初步 zone | 初步 type |
|---|---|---|
| `blog-tired-puffy-eyes.html` | eyes | concept＋routine |
| `blog-jawline-double-chin.html` | jaw-neck | concept＋routine |
| `blog-smile-lines-cheeks.html` | cheeks-nasolabial | concept＋routine |
| `blog-forehead-frown-lines.html` | brows-forehead | concept＋routine |
| `blog-dull-puffy-glow.html` | glow-circulation | concept＋routine |
| `blog-morning-face-ritual.html` | 多部位 | routine（整套儀式） |
| `blog-gua-sha-vs-bojin.html` | 通用 | concept |
| `blog-gua-sha-not-working.html` | 通用 | concept |
| `blog-skip-expensive-facials.html` | 通用 | concept |

觀察：五個部位剛好各有一篇主文＋三篇通用觀念文。回填後就能一眼看出「每個部位其實還很缺深度內容」，這張表直接變成下一批選題清單。

---

## 6. 合規紅線（務必內建，不可違反）

- 只講 tension / rested / refreshed，**不得**出現 cure、lift、lasting、permanent、療效、治療眼疾、TMJ、逆齡等字。
- 結果頁必須標註：「自我感受評估，非醫療診斷」。
- 英文（美國站）內容**不得**出現 meridian / qi / 經絡等 TCM 字眼；中文版（未來發 LINE 用）才可使用。
- 任何要輸出給使用者的內容，`compliance_ok` 必須為 true。

---

## 7. 技術備註

- 內容庫、計分、組裝可以先純前端（JS + JSON）跑起來，不需後端。
- email 收集先留接口，之後接 Systeme.io；「按下寄信 / 發布 / 接帳號」這些動作留給藍玉婷本人，不要自動送出。
- 雷達圖用純 SVG 手繪五軸即可，跟站上茶館風配色一致。
- 一切改動請「只另存新檔、別動原始 9 篇的內容」，除非藍玉婷明確同意。
