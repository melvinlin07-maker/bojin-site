# CLAUDE.md — AI 工作規則（The Bojin Method 美國站）

> 任何 AI／Session 在本專案動手前，先讀本檔 + `PROJECT.md` + `DECISIONS.md`。
> **更新日期：2026-08-08**
> 本檔記錄「你要怎麼跟使用者共事」的規則，是本專案的最高操作守則。

---

## 1. 自主程度：什麼自己做、什麼先問

**可以直接做、做完再回報（低風險、可逆、內部工作）**
- 唯讀盤點、資料整理、分析、找問題、提方案、排優先順序
- 改草稿文案
- 不影響正式環境的本地修改（改完先給使用者看）

**一定要先問、等使用者點頭才動（高風險、對外、正式、不可逆）**
- `git push` / 正式上線
- 修改正在跑的 Landing Page
- 動廣告、動金流
- 動 systeme.io 正式漏斗、n8n 正式 workflow
- 對外發信、發布公開內容
- 修改帳號、權限、DNS、網域、憑證
- 任何可能造成資料遺失、服務中斷或不可逆的操作

**判斷原則**：低風險／可逆／內部 → 自主；高風險／對外／正式／不可逆 → 先問。
**不確定某個東西是否正在使用時，一律先當「不能碰」，先問使用者。**

## 2. 正式環境紅線（沒問過絕對不能碰）
- 正在跑廣告的 Landing Page
- `go.bojinfacetension.com` 正式廣告漏斗
- systeme.io 裡已存在的正式名單、email sequence、表單、漏斗
- n8n 已上線的正式 workflow
- Meta 廣告設定、正式 Pixel／Event 設定
- GA4 / Search Console 等正式追蹤與驗證設定
- GitHub 正式 `main` branch 的直接破壞性修改
- 網域、DNS、CNAME
- 帳號、登入方式、權限、API key、credential
- 正在對外提供的免費影片、PDF、表單與下載入口
- 任何已有真實流量、Lead、名單或客戶資料的頁面與系統

## 3. Git / Deploy 規則
- 網站走 GitHub Pages。可在本地分析、修改檔案、預覽、檢查差異、準備 commit。
- **不要自己直接 push 正式 `main` 上線。**
- push 前先簡短告訴使用者四件事：①改了什麼 ②為什麼改 ③會影響哪些頁面或功能 ④有沒有風險——使用者確認後才 push。
- **多個 AI（Claude Code／ChatGPT／Codex…）會同時碰同一 repo。** 動正式系統或重要檔案前，先查 `git status` 與現有改動，避免覆蓋別的 AI／Session 的工作。只 `git add` 具名檔案，避免 `git add -A`。
- 所有對外動作（廣告、Email、公開貼文、正式漏斗、金流、對客戶發送內容）由使用者最後拍板。

## 4. 溝通方式
- 使用繁體中文，優先白話；一定要用術語時，順手解釋它是什麼。
- 每次先講結論，再講原因。
- 一次只推進一個主要步驟；任務大就拆階段，先講「現在只做哪一步」。
- 主動指出風險、假設與不確定的地方；**已確認事實／推測／建議要分開。**
- 如果使用者明顯被資訊量打亂，收斂回「現在下一步只做什麼」。
- 重要操作前先讓使用者知道，不要突然動正式環境。
- **「做完」的定義**：不只是程式碼寫完，而是使用者知道——做了什麼、為什麼做、目前狀態、下一步是什麼。

## 5. Capability Discovery Rule（原生能力優先）
- 遇到任何需求，**先問「目前的平台本身，有沒有我不知道、但其實已經可以解決這個問題的原生能力？」，再問「怎麼做」。**
- 固定優先順序：①平台原生功能 → ②免費設定 → ③URL／Parameter／Deep Link → ④Event／Webhook／API → ⑤小幅程式修改 → ⑥第三方工具 → ⑦自建系統。
- 多個方案時，先依 ROI／成本／難度／風險排序，只推薦最值得做的前幾個。

## 6. Avoid Overengineering（不要過度開發）
- 市場還沒驗證前，不要把時間花在技術完整度上；不要動不動就延伸到 Database／CRM／Backend／大型自動化。
- 「技術上可以做」不等於「商業上值得做」——所有功能先回到：它是否推進目前最重要的商業驗證（強需求 + 可規模化付費市場）。
- 不要為了顯得完整而把簡單問題複雜化。
- 不要把「有興趣」（點擊／Lead／看影片／完成 Quiz）當成「有購買意願」；最終看真實購買行為。
- 不要把 Meta／GA4 事件數字直接當成真實營收（點擊／Lead／opt-in／Purchase 是不同層級）。

## 7. 文件維護規則
- 三份文件（PROJECT／CLAUDE／DECISIONS）都要標更新日期。
- 內容要分清楚：已確認事實／商業決策／假設／未驗證。
- 不寫任何 secret／token／password／API key。
- 不要讓文件變成「過期真相」；發現與現況不符時，先查證再更新，並更新日期。
